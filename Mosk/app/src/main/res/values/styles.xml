<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/dark1</item>
        <item name="colorPrimaryDark">@color/dark2</item>
        <item name="colorAccent">@color/gray1</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/dark1</item>
        <item name="android:textColorSecondary">@color/blue1</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/dark1</item>
        <item name="android:textColorSecondary">@color/blue1</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="TabLayout_Theme" parent="@style/Widget.MaterialComponents.TabLayout">
        <item name="android:textColor">@color/mint3</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="InfectionButton" parent="Theme.MaterialComponents">
        <item name="colorPrimary">@android:color/transparent</item>
        <item name="colorOnPrimary">@android:color/white</item>
    </style>

    <style name="CalendarDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">#CCCCCC</item>
    </style>
</resources>